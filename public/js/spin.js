// Generated by CoffeeScript 1.6.2
/*
Copyright (c) 2011-2013 Felix Gnass
Licensed under the MIT license
*/


(function() {
  (function(factory) {
    if (typeof exports === "object") {
      return factory(require("jquery"), require("spin"));
    } else if (typeof define === "function" && define.amd) {
      return define(["jquery", "spin"], factory);
    } else {
      if (!window.Spinner) {
        throw new Error("Spin.js not present");
      }
      return factory(window.jQuery, window.Spinner);
    }
  })(function($, Spinner) {
    $.fn.spin = function(opts, color) {
      return this.each(function() {
        var $this, data;

        $this = $(this);
        data = $this.data();
        if (data.spinner) {
          data.spinner.stop();
          delete data.spinner;
        }
        if (opts !== false) {
          opts = $.extend({
            color: color || $this.css("color")
          }, $.fn.spin.presets[opts] || opts);
          return data.spinner = new Spinner(opts).spin(this);
        }
      });
    };
    return $.fn.spin.presets = {
      tiny: {
        lines: 8,
        length: 2,
        width: 2,
        radius: 3
      },
      small: {
        lines: 8,
        length: 4,
        width: 3,
        radius: 5
      },
      large: {
        lines: 10,
        length: 8,
        width: 4,
        radius: 8
      }
    };
  });

}).call(this);
