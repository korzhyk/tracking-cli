// Generated by CoffeeScript 1.6.2
(function() {
  $script.ready(['jquery', 'moment'], function() {
    return setInterval(function() {
      return $('[data-timeago]').each(function(i, el) {
        var $el, m, text, title;

        $el = $(el);
        m = moment($el.data('timeago'));
        if (!m) {
          return;
        }
        $el.text((text = $el.text().replace('{{timeago}}', m.fromNow()) || text) ? void 0 : m.fromNow());
        return $el.attr('title', (title = $el.attr('title')) ? title.replace('{{timeago}}', m.format('LLLL')) || title : m.format('LLLL'));
      });
    }, 1000);
  });

}).call(this);
